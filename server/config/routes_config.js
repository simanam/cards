const item = require('../controllers/controller.js')

module.exports = function(app){
    // app.post('/add', item.add)
    // app.get('/all', item.get)
    // app.delete('/remove/:id', item.delete)
    // app.put('/edit/:id', item.update)
    // app.get('/show/:id', item.show)
}